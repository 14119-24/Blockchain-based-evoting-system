{
  "name": "voting-system-render-launcher",
  "version": "1.0.0",
  "private": true,
  "description": "Render web service launcher for the Voting System frontend",
  "main": "render-server.js",
  "scripts": {
    "build": "node -e \"console.log('No build step is required for this frontend launcher.')\"",
    "render-build": "npm run build",
    "start": "node render-server.js"
  },
  "engines": {
    "node": ">=18 <23"
  }
}
